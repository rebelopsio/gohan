# Autostart applications and services
# https://wiki.hyprland.org/Configuring/Keywords/#executing

# ============================================================================
# ESSENTIAL SERVICES
# ============================================================================

# Waybar (status bar)
exec-once = waybar

# Notification daemon
exec-once = mako

# Wallpaper
exec-once = swaybg -i ~/.config/gohan/wallpaper.jpg -m fill

# Idle management and auto-lock
exec-once = hypridle

# Polkit authentication agent
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# XDG Desktop Portal for Hyprland
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# Clipboard manager (requires wl-clipboard and cliphist)
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# ============================================================================
# SYSTEM TRAY APPLICATIONS
# ============================================================================

# Network manager applet
exec-once = nm-applet --indicator

# Bluetooth manager
exec-once = blueman-applet

# Audio control
exec-once = pasystray

# ============================================================================
# USER APPLICATIONS
# ============================================================================

# Add your own autostart applications below
# Example: exec-once = discord --start-minimized
